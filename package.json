{
  "name": "swastyasaathi-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix server install && npm --prefix web install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "start-server": "npm --prefix server run dev",
    "start-web": "npm --prefix web run dev"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
